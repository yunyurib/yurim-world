import React from "react";
import styled from "styled-components";

export const About = () => {
  return <AboutWrapper>어바웃페이지</AboutWrapper>;
};

const AboutWrapper = styled.div`
  background: pink;
`;
