import React from "react";
import styled from "styled-components";

export const Contact = () => {
  return <ContactWrapper>컨택트페이지</ContactWrapper>;
};

const ContactWrapper = styled.div`
  background: plum;
`;
