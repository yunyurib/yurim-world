import React from "react";
import styled from "styled-components";

export const Develop = () => {
  return <DevelopWrapper>어바웃페이지</DevelopWrapper>;
};

const DevelopWrapper = styled.div`
  background: orange;
`;
